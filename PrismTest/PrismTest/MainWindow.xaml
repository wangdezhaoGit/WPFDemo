<Window x:Class="PrismTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismTest"
        mc:Ignorable="d"
        xmlns:prism="http://prismlibrary.com/"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <ContentControl prism:RegionManager.RegionName="ContentRegionA"></ContentControl>
        </Grid>
        <!--<Button Content="Button"
                HorizontalAlignment="Left"
                Margin="675,28,0,0"
                VerticalAlignment="Top"
                Width="75"
                Click="Button_Click" />-->
        <Grid Grid.Row="1">
            <ContentControl prism:RegionManager.RegionName="ContentRegionB"></ContentControl>
        </Grid>
        <Grid Grid.Row="2">
            <ContentControl prism:RegionManager.RegionName="ContentRegionC"></ContentControl>
        </Grid>
    </Grid>
</Window>
